

```
usage: blah.jar --file1 <arg> --file2 <arg> [-h] [-o <arg>] [-v]
version: null
Compare two APKs and print their differences.

    --file1 <arg>    The first file to compare.
    --file2 <arg>    The second file to compare.
 -h,--human          If set, file sizes will be human readable
 -o,--output <arg>   Directory to save the report
 -v,--verbose        If set, more detailed information will be printed.

Sample usage:

 * blah.jar --file1 ~/tmp/apk1.apk --file2 ~/tmp/apk2.apk

Source code: https://github.com/alt236/apkcompare
Please report issues at https://github.com/alt236/apkcompare/issues
```